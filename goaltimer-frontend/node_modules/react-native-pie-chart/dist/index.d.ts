/// <reference types="react" />
declare module "Wedge" {
    type Props = {
        outerRadius: number;
        startAngle: number;
        endAngle: number;
        fill?: string;
        innerRadius?: number | null | undefined;
    };
    /**
     * Wedge is a React component for drawing circles, wedges and arcs.  Like other
     * ReactART components, it must be used in a <Surface>.
     */
    const Wedge: ({ outerRadius, startAngle, endAngle, fill, innerRadius }: Props) => JSX.Element | null;
    export default Wedge;
}
declare module "Pie" {
    import { ViewStyle, StyleProp } from 'react-native';
    export type Props = {
        angle: number[];
        widthAndHeight: number;
        coverFill: string;
        coverRadius: number;
        doughnut: boolean;
        series: number[];
        sliceColor: string[];
        style?: StyleProp<ViewStyle>;
    };
    const Pie: ({ angle, widthAndHeight, coverFill, coverRadius, doughnut, series, sliceColor, style, }: Props) => JSX.Element;
    export default Pie;
}
declare module "react-native-pie-chart" {
    import { StyleProp, ViewStyle } from 'react-native';
    export type Props = {
        widthAndHeight: number;
        series: number[];
        sliceColor: string[];
        coverFill?: string;
        coverRadius?: number;
        doughnut?: boolean;
        style?: StyleProp<ViewStyle>;
    };
    const PieChart: ({ widthAndHeight, series, sliceColor, coverFill, coverRadius, doughnut, style, }: Props) => JSX.Element;
    export default PieChart;
}
